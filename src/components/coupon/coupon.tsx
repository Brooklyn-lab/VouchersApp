import './coupon.scss';
// import {useAppDispatch, useAppSelector} from '../../hooks';
// import {useNavigate, useParams} from 'react-router-dom';
// import {useEffect, useState} from 'react';
// import {AppRoute} from '../../const';
// import {Coupon} from '../../types/company';

function Coupon():JSX.Element {
	// const {id} = useParams();
	// const navigate = useNavigate();
	// const dispatch = useAppDispatch();
	// const company = useAppSelector(({DATA}) => DATA.selectedCompany);
	// const [currentCoupon, setCurrentCoupon] = useState<Coupon | null>(null);
	//
	// useEffect(() => {
	// 	if (company) {
	// 		if(company.coupons && id) {
	// 			// company.coupons.find((coupon) => coupon.id === Number(id) ? setCurrentCoupon(coupon) : null);
	// 		}
	// 	} else {
	// 		navigate(AppRoute.Main);
	// 	}
	// }, [dispatch, company, id]);
	//
	
	return (
		<></>
	);
}

export default Coupon;
